# Режим: RULES (только правила) | LLM (вызов модели) — для начала оставьте RULES
EXTRACT_MODE=RULES

# Если будете подключать LLM — подставьте свой ключ и модель
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# База (DuckDB — файл). Можно не менять
DUCKDB_PATH=data/rag.duckdb

# Требовать ли passed=true для выдачи данных в API
REQUIRE_QUALITY_PASS=true

# Разное
BATCH_ID=2025-09-14
N_DIALOGS=10000
