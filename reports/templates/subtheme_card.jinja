### Подтема: {{ subtheme }}
Определение: {{ definition }}

Объём: {{ dialog_count }} диалогов / {{ mention_count }} упоминаний ({{ share }}% диалогов)

Кластера:
{% for c in clusters -%}
- {{ c.name }} — {{ c.size }}
{% endfor %}

Примеры цитат:
{% for q in quotes -%}
- "{{ q.text_quote }}" (ID:{{ q.dialog_id }}:{{ q.turn_id }})
{% endfor %}

Сопутствующие темы:
{% for co in coocc -%}
- {{ co.a }} ↔ {{ co.b }}: {{ co.count }}
{% endfor %}
